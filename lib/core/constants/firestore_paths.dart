/// Names of the Firestore collections used by the app.
class FirestorePaths {
  const FirestorePaths._();

  static const users = 'users';
  static const items = 'items';
  static const feedback = 'feedback';
}
